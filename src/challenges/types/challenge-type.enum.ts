export enum ChallengeType {
  MATH = 'math',
  // Declared for the roadmap — generators land in later milestones.
  // Once registered, they flow into BRAIN_DUEL automatically.
  WORD = 'word',
  PATTERN = 'pattern',
  MEMORY = 'memory',
  QUIZ = 'quiz',
}
